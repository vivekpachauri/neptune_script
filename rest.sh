#echo 'var roles = ["default.starship", "SuperAdmin"];for (var i = 0; i < roles.length; i++) {     print({"role": roles[i]}, {"$addToSet": {"users": {"$each": adminIds}}}.toString());}for (var j = 0; j < adminIds.length; j++) {   print({"adminId": adminIds[j]},        {$set: {            "adminId": adminIds[j],            "email": adminEmails[j]        }},        {upsert: true});}'
echo 'var roles = ["default.starship", "SuperAdmin"];for (var i = 0; i < roles.length; i++) {     db.authorization.update({"role": roles[i]}, {"$addToSet": {"users": {"$each": adminIds}}});}for (var j = 0; j < adminIds.length; j++) {   db.adminUser.update({"adminId": adminIds[j]},        {$set: {            "adminId": adminIds[j],            "email": adminEmails[j]        }},        {upsert: true});}'

